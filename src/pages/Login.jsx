import { LoginForm } from 'components/LoginForm/LoginForm';

export default function Login() {
  return (
    <section>
      <div>
        <title>Login</title>
        <LoginForm />
      </div>
    </section>
  );
}
